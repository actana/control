# No environment variables are required to run Actana Control locally —
# `pnpm dev` starts the Panel with none set.
#
# The container deployment reads its own file, and everything in that one is
# optional too: see `deploy/.env.example`.

{
  "name": "@actana/shared",
  "version": "0.4.4",
  "description": "Actana Control shared internals — mutation/query contracts, the blob-registry codec, event log, pairing primitives, and the machine-side modules the Core daemon and the `actana` CLI both use. The core-link frames live in @actana/sdk.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actana/sdk": "workspace:*",
    "@peculiar/x509": "^1.14.2",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.13",
    "@types/node": "25.8.0",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  }
}
